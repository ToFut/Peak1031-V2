# ==========================================
# SUPABASE & PRACTICEPANTHER ENVIRONMENT TEMPLATE
# ==========================================
# Copy this to your .env file and fill in the values

# Supabase Configuration
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Frontend Supabase (for reference)
REACT_APP_SUPABASE_URL=https://your-project-id.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key-here

# PracticePanther OAuth Configuration
PP_CLIENT_ID=your-client-id-from-practicepanther
PP_CLIENT_SECRET=your-client-secret-from-practicepanther
PP_REDIRECT_URI=https://localhost:8000/

# Optional: Custom sync intervals (defaults shown)
SYNC_INTERVAL_MINUTES=15
FULL_SYNC_HOUR=2

# Database connection (if using direct PostgreSQL)
# DATABASE_URL=postgresql://user:password@host:port/database

# ==========================================
# NOTES:
# ==========================================
# 
# 1. SUPABASE_SERVICE_KEY vs SUPABASE_ANON_KEY:
#    - SERVICE_KEY: Backend use, full database access
#    - ANON_KEY: Frontend use, limited by RLS policies
#
# 2. PP_REDIRECT_URI must match exactly what you registered
#    with PracticePanther (including trailing slash if used)
#
# 3. For production, use HTTPS URLs only
#    For development, localhost HTTP is acceptable
#
# 4. Keep these values secret! Add .env to .gitignore
#
# ==========================================